# Search scraper: every 4 hours
0 */4 * * * cd /app && node dist/index.js search >> /var/log/cron-search.log 2>&1

# Enrich scraper: every 4 hours (15 min after search)
15 */4 * * * cd /app && node dist/index.js enrich >> /var/log/cron-enrich.log 2>&1
