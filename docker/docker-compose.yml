services:
  scraper:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: warez-plex-scraper
    restart: unless-stopped
    ports:
      - "${API_PORT:-3000}:3000"
    env_file:
      - ../.env
    volumes:
      - scraper-logs:/var/log
    # Optional: if NocoDB runs in the same Docker network
    # networks:
    #   - nocodb-net

volumes:
  scraper-logs:

# networks:
#   nocodb-net:
#     external: true
#     name: nocodb_default
